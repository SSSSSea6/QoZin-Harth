# 你的数据在火塘是怎么处理的

这份说明写给使用火塘的人：我们保存什么、谁能看到、保存多久。火塘是开源的，每一条都可以在代码里核对，括号里是位置。相关行为一旦改变，这份文件同步修改。

## 我们保存什么

- 账号：昵称、邮箱、密码的哈希值、注册时间。密码本身不保存。（`apps/api/src/db/auth-schema.ts`）
- 登录会话：登录时的 IP 地址和浏览器标识，随会话过期一起删除。目前只是保存，没有用于任何分析。（同上，session 表）
- 圈子：你创建或加入的圈子、加入时间、在圈内的角色。（`apps/api/src/db/schema.ts`）
- 内容：你发的帖子、回复、应答、私聊消息，以及交接后的互评。（同上）
- 开发者资格：申请时写的内容、处理结果与原因；邀请码由谁发出、被谁使用；资格的授予与撤销记录。（`apps/api/src/domain/developers.ts`）
- 工具用量：按工具所有者按天汇总的后端运行次数与时长、写入字节数、发帖数，按月的燃料账户与存储占用快照。只有数字，没有运行内容。（`apps/api/src/tools/fuel.ts`）

目前不收集手机号、学号、位置等信息。将来增加校园认证之类的功能时，会先修改这份文件再上线。

## 谁能看到

- 圈内的帖子和回复：只有该圈成员。
- 公开圈的名字和存在：它上级圈的成员可以看到并加入；私密圈只有成员和拿到邀请链接的人能看到。（`apps/api/src/domain/circles.ts`）
- 私聊：只有对话的两个人。
- 帖子的应答：发布者看到全部，其他人只看到自己的。（`apps/api/src/routes/posts.ts`）
- 个人页：昵称、加入时间、收到的评价，对已登录用户可见。
- 开发者申请的内容和申请者昵称：只有平台管理员。你的邀请码和它们的使用状态：只有你自己；管理员发出的码由谁使用，管理员能看到。你的用量：只有你自己；圈主只看到某个工具的开发者本月额度是否用完。（`apps/api/src/routes/developers.ts`）

运营团队在技术上可以访问数据库，只在处理故障、投诉和法律要求时查看。管理员按用户 id 指定（`HARTH_ADMIN_IDS`），不按邮箱。

## 圈里装的工具能看到什么

- 工具由圈主安装，安装时会看到它申请的权限清单；没有申请的它做不到（`apps/api/src/routes/tool-api.ts`）。可申请的项目：你的 id 和昵称、圈子名称和成员数、成员列表、圈内帖子、以工具名义发帖、保存工具自己的数据，以及按清单里的时间表定时运行。
- 工具的页面在你打开它时运行，用一枚 15 分钟有效的令牌访问这些数据（`apps/api/src/tools/token.ts`）。工具也可以带一段后端代码，在平台的沙箱里运行：没有网络、文件和系统权限，只能用同一套令牌、按同样的权限访问所在圈的数据；每次运行都有内存、时长、调用次数和发帖数的上限（`apps/api/src/tools/executor/`、`apps/api/src/tools/runs.ts`）。
- 定时运行以工具自己的身份进行，发出的帖子不记任何人为作者，显示为工具名。时间表由圈主安装时确认；新版本改了时间表或权限，圈主重新确认前按原来的执行（`apps/api/src/tools/schedules.ts`、`apps/api/src/routes/circle-tools.ts`）。
- 工具的开发者拿不到圈里的数据：真实圈里的运行内容、日志和错误只有该圈圈主能看；开发者只看到按错误类型汇总的次数。开发者调试用的开发会话与正式安装的数据互相隔离（`apps/api/src/routes/tools.ts`）。
- 工具保存的数据按「工具 × 圈」隔离，圈主卸载即清空，定时任务和运行记录一并删除（`apps/api/src/routes/circle-tools.ts`）。
- 工具页里的「反馈」按钮会打开你和这个工具的开发者之间的私聊：只有你点了才会建立，不会把圈里的数据带过去（`apps/web/src/app/c/[id]/t/[slug]/page.tsx`）。
- 发布工具需要开发者资格（邀请码或申请）；每位开发者按资源用量有一份每月免费额度，用完后它的后端运行和写入会停到下月，工具页面和读取照常（`apps/api/src/tools/fuel.ts`）。
- 工具上架前先做自动检查，再由 AI 审核代码与权限声明是否相符；审核只看代码，不接触用户数据（`apps/api/src/tools/review.ts`）。
- AI 拿不准的，以及已上架后需要复核的，由平台管理员人工审核：管理员能看到工具的代码、清单和开发者昵称，也可以在自己所在的圈里试运行它；试运行和普通使用一样，只拿到清单里申请的权限（`apps/api/src/routes/tools.ts`）。

## 保存多久

- 圈子熄灭后，里面的内容只读保留，不会删除；你的评价记录不受影响。（`apps/api/src/domain/lifecycle.ts`）
- 登录会话过期后删除。
- 在自己的个人页可以随时导出全部数据（一个 JSON 文件），也可以注销账号。注销会删除昵称、邮箱、密码、登录记录和设备授权记录，退出所有圈子；你发过的帖子、回复、私聊和评价会以"已注销用户"的名义保留，因为它们也是别人的记录。你是圈主的圈会交给最早加入的成员，没有其他成员就归档。注销时开发者资格作废、未用的邀请码作废、申请内容清空；你发布的工具和它在圈里的数据保留，用量汇总归到匿名账号。（`apps/api/src/domain/account.ts`）
- 开发者申请与处理结果长期保留，注销时清空内容；用量汇总按月保留。
- 数据库每天备份一次到阿里云杭州的对象存储，备份 30 天后自动删除；注销或删除的数据会随之在 30 天内从备份里消失。

## 合规

我们按《个人信息保护法》等相关法律处理个人信息。正式的隐私政策随服务上线发布。对数据处理有疑问，可以在 [Issues](../../issues) 提出。

## 这份文件怎么维护

改动涉及以上任何一条行为的代码时，同一次提交里修改本文件。
